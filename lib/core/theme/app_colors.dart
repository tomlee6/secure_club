import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF16202A); // Main dark background
  static const Color topNavBackground = Color(0xFF1A242D);
  static const Color cardBackground = Color(0xFFFFFFFF); // White cards
  
  // Accents
  static const Color primaryGreen = Color(0xFF06C270); // Main green branding
  static const Color warningOrange = Color(0xFFFF9800);
  static const Color errorRed = Color(0xFFF44336); 
  static const Color actionBlue = Color(0xFF2962FF); // Ban request button
  
  // Texts
  static const Color textDark = Color(0xFF1A242D); // Dark text for white cards
  static const Color textMain = Color(0xFFFFFFFF); // White text for dark backgrounds
  static const Color textMuted = Color(0xFF9E9E9E); // Gray text
  
  // Specific
  static const Color inputBorder = Color(0xFFE0E0E0); // Light border for inputs on cards
  static const Color cardBorder = Color(0xFFEEEEEE);
}
